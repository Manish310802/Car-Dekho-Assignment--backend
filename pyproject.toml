[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# B008: FastAPI's Depends() in defaults is the idiomatic pattern, not a bug.
ignore = ["E501", "B008"]

[tool.pytest.ini_options]
testpaths = ["tests"]
